# Handoff — 2026-05-05

## Open items at end of day

- **Cloudflare in front of GitHub Pages** — still deferred (cost). Re-evaluate after first 60 days of GSC data on the new article wave.
- **Wave-2 article slugs** — `competitor-is-cheaper`, `bad-timing`, `call-me-next-quarter`, `happy-with-current-process` are demand-validated and ready to brief. Do not start until wave-1 has 60 days of GSC indexation data.
- **Sentry + UptimeRobot** — still deferred (ship-the-show priority). 5xx hygiene is in place but no live error tracking.
- **Audio upload magic-byte validation** at `backend/src/routes/calls.js:18-37` — still pending, low-medium severity.
- **GSC indexing** — push the 4 newly-rendered articles (not-interested, send-me-information, need-to-think-about-it, no-budget) into Search Console URL Inspection → Request Indexing manually. Rate-limited ~10/day per property.
- **Mac code signing** — pending Apple Developer approval (enrolled 2026-03-12).

## Log

### early — codex — Gold-standard `too-expensive` page completion
**Touched:** `website/objections/too-expensive.html`, `website/objections/index.html`, `website/objections.html`, `website/sitemap.xml`, `website/llms.txt`, `docs/programmatic-seo/pages/too-expensive.json`, `docs/programmatic-seo/briefs/README.md`, `docs/programmatic-seo/README.md`
**What happened:** Picked up after Claude hit usage limits mid-render. Created canonical hub at `website/objections/index.html`, kept legacy `objections.html` as noindex redirect. Fixed canonical paths for nested `/objections/` location. Created JSON payload at `docs/programmatic-seo/pages/too-expensive.json`. Updated sitemap and llms.
**Decisions:** Use `.html` extensions throughout (matches existing site convention). Hub at `/objections/` initially, later pivoted to `/articles/`.
**Next:** Render the 4 remaining wave-1 article HTMLs.

### early — codex — Drafted JSON payloads for 4 remaining wave-1 articles
**Touched:** `docs/programmatic-seo/pages/{not-interested,send-me-information,need-to-think-about-it,no-budget}.json`, `docs/programmatic-seo/briefs/README.md`
**What happened:** Wrote schema-conforming JSON payloads for all 4 remaining articles. Validated JSON parse, banned-pattern scan, pairwise similarity (max 0.16, well below 70% threshold). Marked all as `reviewed`.
**Decisions:** Render gates on accepting the gold-standard HTML quality bar.
**Next:** Browser-review too-expensive HTML, then render the 4 drafts.

### mid — codex — Pivot from objection hub to articles hub
**Touched:** `website/articles/index.html` (new), `website/objections/index.html` (now redirect), `website/objections.html` (now redirect), `website/objections/too-expensive.html` (breadcrumb + related links updated), `website/llms.txt`, `website/sitemap.xml`, all marketing pages (nav/footer), `docs/programmatic-seo/README.md`, `docs/programmatic-seo/briefs/*.md`
**What happened:** User pointed out the objection hub looked like a sitemap, not a destination. Pivoted to a broader `/articles/` hub with topic lanes (objection handling, discovery, live-call coaching, follow-up, Brutus features, sales tips). Old `/objections/` URLs now redirect.
**Decisions:** Articles is the umbrella; objection handling is one topic lane. Future topics: features and tips. Article URLs stay at `/objections/{slug}.html` for SEO continuity, but link back to `/articles/`.
**Next:** Add DNA CTA path to articles.

### mid — codex — Article CTA structure locked in
**Touched:** `website/objections/too-expensive.html`, `website/articles/index.html`, programmatic-seo docs
**What happened:** Mid-article DNA Test CTA added. Bottom CTA stays as the article-specific Brutus signup CTA. Rule documented across programmatic-seo README, briefs README, and subagent-workflow.
**Decisions:** Every article gets two CTAs — DNA in middle (lead magnet), Brutus signup at bottom (direct). They do not collapse into one.

### mid — codex — Mobile-first rule locked in
**Touched:** `docs/programmatic-seo/README.md`, `docs/programmatic-seo/briefs/README.md`, `docs/programmatic-seo/subagent-workflow.md`, mobile CSS in `website/articles/index.html` and `website/objections/too-expensive.html`
**What happened:** Article and hub mobile layouts tightened (single-column grids, full-width CTAs, better mobile typography). Rule added: every article passes mobile review before approval.

### mid — codex — Site-wide nav made fully transparent
**Touched:** Every marketing/article HTML page (nav `<style>` blocks)
**What happened:** Removed opaque nav background, blur, border, and shadow. Desktop nav now fully transparent so the neural network animation is visible behind it. Mobile dropdown stays dark for readability when open.

### mid — codex — Article header overlap fix + remove second nav strip
**Touched:** `website/objections/too-expensive.html`, `website/articles/index.html`
**What happened:** Removed in-article breadcrumb nav strip. Added top padding so article H1 starts below the fixed global nav.
**Decisions:** Article pages do not get a secondary breadcrumb/nav bar.

### mid — codex — Brand casing fix: brutus → Brutus
**Touched:** Marketing site (all pages), frontend app (auth pages, index.html), desktop renderer (overlay.html, main.html), backend route messages and email templates, docs/programmatic-seo/pages/*.json, voice reference, JSON payloads
**What happened:** Capitalized "B" in "Brutus" across every user-facing surface (marketing, app, desktop, API messages, emails). Left technical identifiers lowercase (domains, package names, CSS classes, IPC bridges, file names).

### mid — codex — Privacy policy: HIPAA limitation + GDPR rewrite
**Touched:** `website/privacy.html`
**What happened:** Added HIPAA/PHI section stating Brutus is not intended for PHI unless a signed BAA exists. Then rewrote the full policy in GDPR-style: controller, data categories, purposes, lawful bases, AI/call processing, processors, international transfers, retention, user rights, export/deletion, security, cookies, children, HIPAA limitation, change notice, contact.
**Decisions:** Brutus does not claim HIPAA compliance. No automated BAA flow until the underlying program (risk analysis, safeguards, vendor review, breach process) is built.

### mid — codex — Commit `a04a1d2` pushed: "Add article hub and privacy updates"
**Touched:** 55 files
**What happened:** First commit of the day pushed to `main`. Excluded pre-existing dirty files (`backend/src/scripts/seedFromYouTube.js`, lariosco/, package-lock).

### late — codex — Render 4 remaining wave-1 articles
**Touched:** `website/objections/{not-interested,send-me-information,need-to-think-about-it,no-budget}.html` (new), `website/articles/index.html` (links updated), `website/sitemap.xml`, `website/llms.txt`
**What happened:** Rendered all 4 reviewed JSON payloads to full HTML pages. Hub article rows updated from review placeholders to live links. Sitemap and llms updated.

### late — codex — Loosened article template constraints
**Touched:** `docs/programmatic-seo/{README.md,subagent-workflow.md,page-brief-template.md,page-schema.json}`, `docs/programmatic-seo/briefs/too-expensive.md`, `docs/programmatic-seo/pages/too-expensive.json`, `website/objections/too-expensive.html`
**What happened:** Removed the implicit "always 3 flavors" assumption. `page-schema.json` `counter_frames` `minItems` dropped from 3 to 1. New rule: identify the actual number of meanings the objection has, no forced count. Correction sections must include the better move, not just name the mistake. Updated too-expensive page to use "meaning" language.
**Decisions:** Objection pages can have 1, 2, 3, or more "meanings" — driven by the objection, not the template.
**Next:** Apply the loosened rule to the other 4 rendered articles.

### late — claude — Wired SEO playbook into article-writing rules (4 places)
**Touched:** `docs/seo/{playbook,technical,content}.md` (new — copied from Claude memory), `docs/programmatic-seo/README.md`, `docs/programmatic-seo/subagent-workflow.md`, `docs/programmatic-seo/page-brief-template.md`, `docs/programmatic-seo/briefs/README.md`
**What happened:** Copied the SEO playbook + technical reference + content reference into the repo at `docs/seo/` so Codex and sub-agents can read them (Claude memory is not visible to Codex). Wired as a hard rule in 4 places: programmatic-seo README, subagent-workflow (new "Mandatory SEO playbook reference" section), brief template, briefs index.
**Decisions:** SEO playbook is now a third hard requirement alongside the article-writing skill and the voice reference. Per-page programmatic-content requirements (~1200-1500 words, ≥70% unique, sibling links, byline, schemas, do-not list) apply to every draft.

### late — claude — De-templatized the 4 rendered articles
**Touched:** `website/objections/{not-interested,send-me-information,need-to-think-about-it,no-budget}.html`
**What happened:** Replaced rigid "flavor 01/02/03" framing with the actual meaning count for each objection.
- not-interested: 2 meanings (interruption reflex, genuine bad fit)
- send-me-information: 2 meanings (polite brush-off, real but unfocused interest)
- need-to-think-about-it: 3 blockers (price, authority, trust) — kept count, renamed "flavor" to "blocker"
- no-budget: 2 meanings (authority deflection, real spend constraint)
Common mistakes sections rewritten as "common mistakes and fixes" with the better move on each line.

### late — claude — Person author schema + visible bylines on all 5 articles
**Touched:** All 5 article HTMLs (Article JSON-LD `author` block + new visible byline + `.byline` CSS), all 5 JSON payloads (`author` field), `docs/programmatic-seo/briefs/README.md`
**What happened:** Replaced Organization author with Person schema (Isaiah Rios, founder, Brutus AI, with worksFor and url to /about.html). Added visible byline `by Isaiah Rios, founder of Brutus AI` below every H1, linking to /about.html. Brief README byline placeholder replaced with locked rule.
**Decisions:** E-E-A-T per SEO playbook requires Person schema. Founder byline is the right call.

### late — claude — Synced JSON payloads to match published HTML
**Touched:** `docs/programmatic-seo/pages/{too-expensive,not-interested,send-me-information,need-to-think-about-it,no-budget}.json`
**What happened:** All 5 payloads moved from `reviewed` to `published`. Author fields updated to `Isaiah Rios / Brutus AI`. `common_mistakes` arrays expanded to include the fix on each line, matching the deepened HTML.

### late — claude — Commit `01f1796` pushed: "Render wave-1 articles, wire SEO playbook, add Person bylines"
**Touched:** 22 files, +1,752/-102
**What happened:** Pushed to main. Excluded pre-existing dirty files (seedFromYouTube.js, package-lock, lariosco/).

### late — claude — Established handoff protocol
**Touched:** `docs/handoffs/README.md` (new), `docs/handoffs/2026-05-05.md` (this file), `~/.claude/projects/C--Users-isaia/memory/feedback-handoff-protocol.md` (new)
**What happened:** Created the shared date-based handoff protocol. Both Claude and Codex append to `docs/handoffs/YYYY-MM-DD.md` after every completed task. New day → new file. Memory and handoff are separate layers (handoff = chronological repo doc; memory = semantic per-agent context).
**Decisions:** Single handoff per day, both agents append. Read latest handoff cover-to-cover at session start.
